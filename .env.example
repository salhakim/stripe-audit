# stripe-audit reads a Stripe key from STRIPE_SECRET_KEY (or the --key flag).
# STRONGLY RECOMMENDED: a read-only RESTRICTED key (rk_...), never your full secret key.
# stripe-audit is READ-ONLY — it never creates, updates, or deletes anything in Stripe.
STRIPE_SECRET_KEY=rk_test_your_restricted_readonly_key_here
